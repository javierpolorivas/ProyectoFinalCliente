---
const { title, school, description, className, imageSrc } = Astro.props;
---

<div class="flex flex-col gap-y-4 group md:flex-row md:space-x-6 md:space-y-0 p-4 rounded-lg border-gray-300 ">
  <!-- Imagen de la carta -->
  <div class="w-full md:w-1/3">
    <div class="relative flex items-center justify-center rounded-lg overflow-hidden">
      <img src={imageSrc} alt={school} class="object-cover w-full h-32 md:h-40 rounded-lg shadow-md border border-black shadow-lg hover:shadow-2xl transition-all duration-300 ease-in-out transform hover:scale-105 border-2 border-gray-200" loading="lazy" />
    </div>
  </div>

  <!-- Contenido de la carta -->
  <div class="w-full md:w-2/3 text-left">
    <h3 class="text-xl font-bold text-gray-800">{title}</h3>
    <p class="text-md font-semibold text-gray-600 mt-1">{school}</p>
    <p class="text-sm mt-3 text-gray-700 leading-relaxed">{description}</p>
  </div>
</div>



